package com.example.demo.dom.all.mapper;

import com.example.demo.dom.all.entity.ModuleRole;

public interface ModuleRoleMapper {
    int insert(ModuleRole record);

    int insertSelective(ModuleRole record);
}